import { NavLink } from "react-router-dom";
import LandingNav from "./LandingNav";

const Landing = () => {
  return (
    <>
      <section>
        <LandingNav />
      </section>
    </>
  );
};

export default Landing;
